$patterns:
  phone: '/^\+?[\d\s\-]{7,20}$/'

contact:
  $any_of:
    - $type: String
      $format: email
    - $type: String
      $format: phone
size:
  $any_of:
    - $type: String
      $enum: [small, medium, large]
    - $type: Integer
      $gte: 1
      $lte: 100
