shipment:
  $one_of:
    - $required: [delivery]
      delivery:
        $required: [address]
        address: string
        carrier: string
        status: string, in [pending, shipped, delivered, returned]
    - $required: [pickup]
      pickup:
        $required: [location]
        location: string
        slot: string
