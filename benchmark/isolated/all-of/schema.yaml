product:
  $all_of:
    - name: string, size [2, 80]
      price: number, >= 0
    - sku: string, size [6, 20]
      stock: integer, >= 0
    - category: string, enum [electronics, clothing, food, books]
      active: boolean
