$patterns:
  slug: /^[a-z0-9]+(?:-[a-z0-9]+)*$/

$required: [data]

data:
  $required: [id, title, slug, body, published]

  id: Integer {>= 1}
  title: String {1, 120}
  slug: String (slug)
  body: String {1, }
  published: Boolean

meta: any
